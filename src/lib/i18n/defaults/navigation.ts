import type {
  BreadcrumbLabels, CommandPaletteLabels,
  SectionNavLabels, StepperLabels,
} from "../types";

export const breadcrumbDefaults: BreadcrumbLabels = {
  breadcrumb: "Breadcrumb",
};

export const commandPaletteDefaults: CommandPaletteLabels = {
  noCommandsFound: "No commands found",
};

export const sectionNavDefaults: SectionNavLabels = {
  sectionNavigation: "Section navigation",
};

export const stepperDefaults: StepperLabels = {
  progress: "Progress",
};
